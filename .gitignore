# --- Build Artifacts (The Law: No Local Builds) ---
build/
dist/
*.spec
# We keep DocRefinePro.spec because GitHub Actions needs it for Windows
!DocRefinePro.spec

# --- Runtime Data ---
Workspaces/
__pycache__/
*.py[cod]
*.tmp

# --- Logs ---
*.log
app_events.jsonl

# --- Environment & Configs ---
.env
.venv/
env/
.vscode/
.idea/
.DS_Store

# --- Duplicate handling ---
# Ignore the root version file if you accidentally recreate it
/docrefine_version.json

# --- Gist Versioning Folder ---
# CRITICAL FIX: Ignore this folder so the Main Repo doesn't 
# try to treat it as a broken submodule during CI/CD.
53752cda3c39550673fc5dafb96c4bed/